http:
  routers:
    whoami:
      rule: "Host(`whoami.{{ base_url }}`)"
      service: "whoami-service"

  services:
    whoami-service:
      loadBalancer:
        servers:
          - url: "http://reverse_proxy_whoami:80"
